# 서버 환경(conda: openalex_vdb)에 설치된 버전으로 고정.
# 갱신하려면:  python scripts/freeze.py > requirements.txt
#
# python >= 3.10  (langchain-qdrant 1.x 요구사항. 서버는 3.13.x)
#
# 서버 GPU: NVIDIA A6000 (Ampere, sm_86) / CUDA 12.4
#
# torch 는 로컬 버전까지 못박아 뒀다 (2.13.0+cu124).
# 그냥 2.13.0 으로 두면 pip 가 PyPI 의 CPU 전용 휠을 집어갈 수 있다.
# 실제로 그렇게 조용히 +cpu 로 갈아끼워진 적이 있어서 이렇게 고정한다.
#
# 설치 후 반드시 확인:
#   python -c "import torch; print(torch.__version__, torch.version.cuda, torch.cuda.is_available())"
#   -> 2.13.0+cu124 12.4 True
#
# GPU 가 없는 머신이면 아래 torch 줄을 2.13.0+cpu 로 바꾸고
# --extra-index-url 줄은 지운다.
#
# 역할별로 필요한 것만 깔아도 됩니다:
#   preprocess.py --stage filter/abstract  -> [공통] + [전처리]
#   preprocess.py --stage embed            -> [공통] + [임베딩/리랭커]
#   post.py                                -> [공통] + [Qdrant]
#   api.py                                 -> 전부
#   scripts/client.py                      -> 아무것도 필요 없음 (표준 라이브러리만)

# ---------------------------------------------------------------- [공통]
numpy==2.4.6
tqdm==4.70.0
orjson==3.11.9

# -------------------------------------------------------------- [전처리]
pyarrow==15.0

# ------------------------------------------------------- [임베딩 / 리랭커]
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.13.0+cu124            # 로컬 태그까지 고정해야 CPU 휠로 안 바뀐다
sentence-transformers==5.7.0   # CrossEncoder(리랭커)도 여기서 온다
transformers==5.15.0
tokenizers==0.22.2
huggingface-hub==1.27.0
langchain-huggingface==1.2.2
langchain-core==1.5.4

# --------------------------------------------------------------- [Qdrant]
langchain-qdrant==1.1.0
qdrant-client==1.19.0
pydantic==2.13.4

# ------------------------------------------------------------------ [API]
fastapi==0.141.1
uvicorn==0.52.1
starlette==1.6.0
